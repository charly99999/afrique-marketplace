import { useAuth } from "@/_core/hooks/useAuth";
import { MarketplaceShell } from "@/components/MarketplaceShell";
import { fileToDataUrl } from "@/lib/media";
import { trpc } from "@/lib/trpc";
import { ImagePlus, LockKeyhole, Video, X } from "lucide-react";
import { useState } from "react";
import { Link } from "wouter";

const categories = ["immobilier", "vehicules", "telephones", "electronique", "mode", "emploi"] as const;

export default function Sell() {
  const { isAuthenticated } = useAuth();
  const profile = trpc.marketplace.profile.mine.useQuery(undefined, { enabled: isAuthenticated });
  const [media, setMedia] = useState<string[]>([]);
  const [form, setForm] = useState({ title: "", description: "", category: "immobilier" as typeof categories[number], city: "", price: "", currency: "XOF" as "XOF" | "XAF" | "MAD" | "CDF" | "GNF" | "USD", condition: "bon_etat" as "neuf" | "comme_neuf" | "bon_etat" | "a_reparer" });
  const create = trpc.marketplace.listings.create.useMutation({ onSuccess: () => { setForm({ ...form, title: "", description: "", price: "" }); setMedia([]); } });
  const chooseMedia = async (event: React.ChangeEvent<HTMLInputElement>) => {
    const files = Array.from(event.target.files ?? []).slice(0, 4 - media.length);
    const data = await Promise.all(files.map(fileToDataUrl));
    setMedia([...media, ...data]);
  };
  const submit = (event: React.FormEvent) => { event.preventDefault(); create.mutate({ ...form, price: Number(form.price), mediaData: media }); };

  if (!isAuthenticated || !profile.data) return <MarketplaceShell title="Publier"><section className="page-wrap section-space"><div className="gate-card"><LockKeyhole size={28} /><h2>Votre profil est nécessaire pour publier.</h2><p>Inscrivez-vous sans e-mail puis vérifiez votre identité avant de diffuser une annonce.</p><Link href="/profil" className="button button--gold">Créer mon profil</Link></div></section></MarketplaceShell>;
  if (profile.data.verificationStatus !== "verified") return <MarketplaceShell title="Publier"><section className="page-wrap section-space"><div className="gate-card"><LockKeyhole size={28} /><h2>La publication attend votre vérification.</h2><p>Votre profil doit être vérifié afin de protéger les acheteurs et vendeurs.</p><Link href="/verification" className="button button--gold">Vérifier mon identité</Link></div></section></MarketplaceShell>;
  return <MarketplaceShell title="Créer une annonce"><section className="page-wrap section-space sell-layout"><div className="sell-side"><p className="eyebrow eyebrow--dark">Une publication de qualité</p><h2>Présentez l’essentiel, avec clarté.</h2><p>Votre profil vérifié rassure les personnes intéressées. Décrivez honnêtement l’état, la localisation et le prix de votre annonce.</p></div><form className="premium-form listing-form" onSubmit={submit}><div className="form-row"><label>Titre<input required minLength={6} maxLength={140} value={form.title} onChange={e => setForm({ ...form, title: e.target.value })} placeholder="Ex. Appartement lumineux à louer" /></label><label>Catégorie<select value={form.category} onChange={e => setForm({ ...form, category: e.target.value as typeof form.category })}>{categories.map(category => <option key={category}>{category}</option>)}</select></label></div><label>Description<textarea required minLength={20} value={form.description} onChange={e => setForm({ ...form, description: e.target.value })} placeholder="Décrivez l’offre, ses atouts et les conditions importantes…" rows={5} /></label><div className="form-row"><label>Ville<input required value={form.city} onChange={e => setForm({ ...form, city: e.target.value })} placeholder="Ex. Dakar" /></label><label>État<select value={form.condition} onChange={e => setForm({ ...form, condition: e.target.value as typeof form.condition })}><option value="neuf">Neuf</option><option value="comme_neuf">Comme neuf</option><option value="bon_etat">Bon état</option><option value="a_reparer">À réparer</option></select></label></div><div className="form-row"><label>Prix<input required min="0" type="number" value={form.price} onChange={e => setForm({ ...form, price: e.target.value })} /></label><label>Devise<select value={form.currency} onChange={e => setForm({ ...form, currency: e.target.value as typeof form.currency })}><option value="XOF">XOF</option><option value="XAF">XAF</option><option value="MAD">MAD</option><option value="CDF">CDF</option><option value="GNF">GNF</option><option value="USD">USD</option></select></label></div><div className="media-picker"><div><strong>Photos ou courte vidéo</strong><span>Jusqu’à 4 médias. La qualité est préservée dans le stockage sécurisé.</span></div><label className="button button--outline button--small"><ImagePlus size={16} /> Photos<input type="file" accept="image/jpeg,image/png,image/webp" multiple onChange={chooseMedia} hidden /></label><label className="button button--outline button--small"><Video size={16} /> Vidéo<input type="file" accept="video/mp4,video/webm" onChange={chooseMedia} hidden /></label></div>{media.length > 0 && <div className="media-preview-grid">{media.map((item, index) => <div key={`${item.slice(0, 25)}-${index}`}><button type="button" onClick={() => setMedia(media.filter((_, position) => position !== index))}><X size={15} /></button>{item.startsWith("data:video") ? <video src={item} /> : <img src={item} alt="Prévisualisation de l’annonce" />}</div>)}</div>}{create.error && <p className="form-error">{create.error.message}</p>}{create.isSuccess && <p className="form-success">Votre annonce est publiée et visible dans la marketplace.</p>}<button disabled={create.isPending} className="button button--gold button--wide">{create.isPending ? "Publication…" : "Publier mon annonce"}</button></form></section></MarketplaceShell>;
}
