import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.afriquemarketplace.app',
  appName: 'Afrique Marketplace',
  webDir: 'dist/cloudflare'
};

export default config;
